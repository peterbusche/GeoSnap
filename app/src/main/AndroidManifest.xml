<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapApp2"
        tools:targetApi="31">

        <!-- Declare the MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Snapshot activity -->
        <activity android:name=".SnapshotHandler" />

    </application>

</manifest>


<!--
Declares App Components:
    Registers components like activities, services, broadcast receivers, and content providers.
    Each component must be declared here for the Android system to recognize it.

Specifies App Configuration:
    Defines permissions required by the app (e.g., internet access, camera usage).
    Configures app-level properties like themes, API levels, and hardware features.

Sets the Entry Point:
    Specifies the main (launcher) activity that is launched when the app starts.

Controls App Visibility and Behavior:
    Determines which components can interact with other apps via attributes like android:exported.
    Defines intent filters to specify how the app responds to system or app-level events.




If They Are New Activities:
    Each Activity must be declared in the AndroidManifest.xml file.



-->